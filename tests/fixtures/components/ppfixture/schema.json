{
  "component": "ppfixture",
  "description": "TEST FIXTURE ONLY - never registered in production. A minimal registry member that write-path suites use as a plain filler band. Its slot map and recipes were deleted at #1101 with the style-slot engine, its `theme` prop at #1111 with the `--dark`/`--inverted` vocabulary, and its `background_image` prop at #1108 when the #705 guard it hosted was retired by decision. See tests/fixtures/components/ppfixture/README.md for who still uses it and the death condition.",
  "props": {
    "id": {
      "type": "string",
      "required": false,
      "description": "Stable band id, so the fixture behaves like any other band at the write path.",
      "default": ""
    },
    "title": {
      "type": "string",
      "required": false,
      "description": "A heading, so the fixture renders something when the hosted claims need a band on the page.",
      "default": ""
    },
    "items": {
      "type": "array",
      "required": true,
      "description": "Makes the component composable (every composable component must declare a required prop - SchemaValidationTest pins that invariant). Carries no `item_fields`: that key was declared here and read by no engine code, and it was deleted at #1101 with the rest of the inert vocabulary."
    }
  },
  "styling": {
    "root_class": "ppfixture",
    "variant_classes": [],
    "tokens": [
      "--pp-band-padding",
      "--color-text",
      "--color-muted",
      "--color-accent"
    ]
  },
  "safe_to_edit": [
    "props"
  ],
  "do_not_touch": []
}
