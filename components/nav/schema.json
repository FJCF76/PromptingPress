{
  "component": "nav",
  "description": "Site header with logo, primary navigation menu, and hamburger toggle on mobile. Reads a registered WP nav menu by theme location. Template-owned chrome (issue 223): it is rendered once by the theme, never composed. It is styled through the `nav` entry of the pp_site_udc site option (update_site_option) using the roles below — the same engine, grammar, groups, @token references, breakpoints, :hover/:focus-visible/:active state maps and presets a band's `udc` takes. It declares ZERO style props and zero style slots; the props below are content and configuration only. STATE PAIRING (#992): setting a role's colour at REST also cancels that role's built-in hover treatment, because the authored block outranks the stylesheet in every state. Whenever you set `typography.color` on `link`, `logo` or `toggle`, set its `:hover` colour in the same write — and when you set `link`, set `link-current` too, or the visitor loses both the hover feedback and the you-are-here marker.",
  "props": {
    "location": {
      "type": "string",
      "required": false,
      "default": "primary",
      "description": "WP theme location slug for the nav menu."
    },
    "logo_text": {
      "type": "string",
      "required": false,
      "default": "",
      "description": "Logo text wordmark. Falls back to pp_site_title() when empty. Shown when no logo image resolves. TEMPLATE CONTRACT (issue 582): NOT reachable from any supported surface — base.php never passes it and a page cannot compose a nav, so it always resolves to the site title in practice. It is documented here because pp_resolve_logo() accepts it, not because you can set it. To change the wordmark, change the site title.",
      "conditionality_note": "NO logo image resolves — a negation the grammar cannot express; with a valid logo_id the image wins and this wordmark is never rendered"
    },
    "logo_id": {
      "type": "number",
      "required": false,
      "default": 0,
      "description": "Media Library attachment ID for the logo image (NOT a URL). When set to a valid image it renders an img tag instead of the text wordmark. Resolution order: this prop, then the pp_logo_id site option, then the WP custom_logo theme-mod. TEMPLATE CONTRACT (issue 582): NOT reachable as a prop — base.php never passes it and a composition naming nav is rejected outright with template_owned_component, so the prop-level image check can never actually run on it. Set the logo through the pp_logo_id SITE OPTION (update_site_option); that is the write the image-attachment rule validates — a non-image or non-existent ID is rejected there."
    },
    "logo_alt": {
      "type": "string",
      "required": false,
      "default": "",
      "description": "Alt text for the logo image. Defaults to the attachment's own alt metadata, then logo_text/site title; it is never empty. TEMPLATE CONTRACT (issue 582): template-supplied, not page-authored — base.php maps the pp_logo_alt SITE OPTION onto this prop, so update_site_option with key pp_logo_alt is the surface. Use it only when the alt should differ from the attachment's own alt metadata; leave it unset otherwise and the attachment's alt is used. The same option also feeds the footer logo. A value that is empty OR WHITESPACE-ONLY counts as unprovided and falls through to the rest of the chain rather than rendering — a blank alt would announce nothing and would suppress the attachment's own alt. A real value renders verbatim, surrounding spaces included.",
      "conditionality_note": "a logo IMAGE resolves, i.e. logo_id points at a real image attachment — a numeric-prop precondition the grammar, whose `present` predicate covers strings and arrays only, cannot express"
    }
  },
  "roles": {
    "_band": {
      "selector": "",
      "description": "The <header class=\"site-header\"> itself — the whole header bar. Set the site header's background and border here.",
      "groups": [
        "typography",
        "spacing",
        "border",
        "background",
        "sizing",
        "shadow",
        "motion"
      ],
      "defaults": {}
    },
    "container": {
      "selector": ".nav__container",
      "description": "The header ROW: the flex line holding the logo, the hamburger and the menu. Set the row's height with sizing.min-height and the space between its items with spacing.gap — those two are why this role exists. This is the row, not the bar: the bar's own background and border are the _band role, so a background here paints an inset band inside the header rather than the header itself. Typography set here only reaches text that does not set its own colour, and every text element in the row (logo, links, the toggle) has its own role — so set colour on those roles, not on this one.",
      "groups": [
        "typography",
        "spacing",
        "border",
        "background",
        "sizing",
        "motion"
      ],
      "defaults": {}
    },
    "logo": {
      "selector": ".nav__logo",
      "description": "The logo link: the wordmark when no logo image resolves, and the link wrapping the image when one does. Setting typography.color here cancels the built-in accent hover (#992) — set a :hover colour in the same write, or hovering the logo does nothing at all.",
      "groups": [
        "typography",
        "spacing",
        "border",
        "background",
        "sizing",
        "motion"
      ],
      "defaults": {}
    },
    "logo-image": {
      "selector": ".nav__logo-image",
      "description": "The logo <img>, when a logo image attachment resolves. Size it here.",
      "groups": [
        "typography",
        "spacing",
        "border",
        "background",
        "sizing",
        "motion"
      ],
      "defaults": {}
    },
    "menu": {
      "selector": ".nav__menu",
      "description": "The menu container. On phones this is the disclosure panel the hamburger opens, so its background is what a mobile visitor sees behind the links.",
      "groups": [
        "typography",
        "spacing",
        "border",
        "background",
        "sizing",
        "shadow",
        "motion"
      ],
      "defaults": {}
    },
    "submenu": {
      "selector": ".nav__menu .sub-menu",
      "description": "The desktop dropdown panel for a menu item that has children. It is a separate surface from `menu`: the mobile disclosure panel and the desktop dropdown used to share one colour knob and fall back differently, which is exactly the hidden coupling that made the old surface hard to reason about.",
      "groups": [
        "typography",
        "spacing",
        "border",
        "background",
        "sizing",
        "shadow",
        "motion"
      ],
      "defaults": {}
    },
    "link": {
      "selector": ".nav__menu ul li a",
      "description": "Every nav link at rest. Use the :hover, :focus-visible and :active states here for the interaction treatment. Setting typography.color here cancels BOTH the built-in accent hover and the current-page accent (#992), because an authored value outranks the stylesheet in every state: set a :hover colour AND the link-current role in the same write.",
      "groups": [
        "typography",
        "spacing",
        "border",
        "background",
        "sizing",
        "motion"
      ],
      "defaults": {}
    },
    "link-current": {
      "selector": ".nav__menu ul li.current-menu-item a",
      "description": "The link for the page the visitor is on. WordPress marks it current-menu-item on every item it considers current — that class is a superset of current_page_item and of aria-current=\"page\", which core only ever sets alongside it — so this one selector reaches them all. Structural CSS gives it bold weight. Set this whenever you set `link`, or the you-are-here marker collapses into the plain link colour (#992).",
      "groups": [
        "typography",
        "spacing",
        "border",
        "background",
        "sizing",
        "motion"
      ],
      "defaults": {}
    },
    "toggle": {
      "selector": ".nav__toggle",
      "description": "The hamburger button, shown on phones only. Its two icons are swapped by structural CSS, not by anything set here, and they inherit this role's colour. Setting typography.color here cancels the built-in accent hover (#992) — pair it with a :hover colour.",
      "groups": [
        "typography",
        "spacing",
        "border",
        "background",
        "sizing",
        "motion"
      ],
      "defaults": {}
    }
  },
  "styling": {
    "root_class": "site-header",
    "variant_classes": [],
    "tokens": [
      "--color-bg",
      "--color-text",
      "--color-accent"
    ],
    "udc_roles": [
      "_band",
      "container",
      "logo",
      "logo-image",
      "menu",
      "submenu",
      "link",
      "link-current",
      "toggle"
    ]
  },
  "safe_to_edit": [
    "nav.php",
    "../../assets/css/components.css (COMPONENT: nav section)"
  ],
  "do_not_touch": [
    "schema.json without updating this component's README.md and the repo-root AI_CONTEXT.md"
  ]
}
