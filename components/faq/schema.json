{
  "component": "faq",
  "description": "FAQ accordion using native HTML details/summary elements. Zero JavaScript required. Each item has a question and an HTML answer. Always emits a FAQPage JSON-LD structured-data block alongside the visible markup.",
  "props": {
    "id": {
      "type": "string",
      "required": false,
      "default": "",
      "description": "Optional HTML id attribute for anchor linking (e.g. navigation anchors). Also becomes the component's stable id."
    },
    "title": {
      "type": "string",
      "required": false,
      "default": "Frequently Asked Questions",
      "description": "Section heading displayed above the FAQ list."
    },
    "title_accent": {
      "type": "string",
      "required": false,
      "default": "",
      "description": "Optional exact substring of title to render in an accent color. Must match title exactly (case-sensitive) or it is ignored."
    },
    "eyebrow": {
      "type": "string",
      "required": false,
      "default": "",
      "description": "Optional short kicker/label rendered as a pill above the title."
    },
    "theme": {
      "type": "enum", "strict": true,
      "values": ["default", "muted", "inverted"],
      "aliases": ["dark"],
      "required": false,
      "default": "default",
      "description": "Background color/tone for this band. 'default' = the band's own default background (no tone override). 'muted' = light tinted surface band (renders the --color-surface background with framing top and bottom borders). 'inverted' = inverted dark background for strong contrast; use 'inverted' for a genuinely dark band."
    },
    "items": {
      "type": "array",
      "item_type": "object",
      "required": true,
      "description": "Array of FAQ items. Each item: { question: string, answer: string (HTML allowed) }.",
      "items": {
        "question": { "type": "string", "required": true, "description": "The question text shown in the accordion summary. Plain text — HTML is escaped." },
        "answer":   { "type": "string", "required": true, "description": "The answer body. Rich HTML (sanitized via wp_kses_post): block-level markup, lists, and links are allowed." }
      }
    }
  },
  "styling": {
    "root_class": "faq",
    "variant_classes": ["faq--dark", "faq--inverted"],
    "tokens": ["--space-xl", "--space-2xl", "--color-border", "--color-surface"],
    "style_slots": {
      "--faq-padding-top": { "type": "length", "default": "var(--pp-band-padding)", "description": "Top padding of the band. Defaults to the shared symmetric band rhythm (fluid ~68-80px desktop, ~54px mobile); set an explicit length to override." },
      "--faq-padding-bottom": { "type": "length", "default": "var(--pp-band-padding)", "description": "Bottom padding of the band. Defaults to the shared symmetric band rhythm; set an explicit length to override." },
      "--faq-bg": { "type": "gradient", "default": "var(--color-surface)", "description": "Section background color or gradient" },
      "--faq-item-bg": { "type": "gradient", "default": "var(--color-bg)", "description": "Individual accordion item background color or gradient", "applies_when": [{"prop": "items", "present": true}] },
      "--faq-eyebrow-color": { "type": "color", "default": "var(--color-text)", "description": "Eyebrow pill text color", "applies_when": [{"prop": "eyebrow", "present": true}] },
      "--faq-eyebrow-bg": { "type": "color", "default": "var(--color-surface-accent)", "description": "Eyebrow pill background color", "applies_when": [{"prop": "eyebrow", "present": true}] },
      "--faq-eyebrow-radius": { "type": "length", "default": "3px", "description": "Eyebrow pill corner radius. Set a large value (e.g. 999px) for a fully rounded pill.", "applies_when": [{"prop": "eyebrow", "present": true}] },
      "--faq-eyebrow-border-width": { "type": "length", "default": "0", "description": "Eyebrow pill border width. Set a non-zero width (with --faq-eyebrow-border-color) for an outlined pill; 0 (default) renders no border.", "applies_when": [{"prop": "eyebrow", "present": true}] },
      "--faq-eyebrow-border-color": { "type": "color", "default": "transparent", "description": "Eyebrow pill border color. Takes effect when --faq-eyebrow-border-width is non-zero.", "applies_when": [{"prop": "eyebrow", "present": true}] },
      "--faq-eyebrow-text-transform": { "type": "text-transform", "default": "uppercase", "description": "Eyebrow pill letter-casing. Default uppercase renders the kicker in caps; set none to keep it as authored (e.g. sentence case), or lowercase / capitalize.", "applies_when": [{"prop": "eyebrow", "present": true}] },
      "--faq-heading-size": { "type": "length", "default": "1.875rem", "description": "Section heading font size", "applies_when": [{"prop": "title", "present": true}] },
      "--faq-heading-color": { "type": "color", "default": "var(--color-text)", "description": "Section heading text color", "applies_when": [{"prop": "title", "present": true}] },
      "--faq-heading-measure": { "type": "length", "role": "measure", "default": "var(--measure-heading)", "description": "Maximum width of the FAQ section heading. Routes the shared --measure-heading design token (40rem) by default, so retuning that ONE token changes every routed band heading at once; set a length here only when THIS band must differ from the rest of the site.", "applies_when": [{"prop": "title", "present": true}] },
      "--faq-body-measure": { "type": "length-or-none", "role": "measure", "default": "none", "description": "Maximum width of an FAQ ANSWER. Default \"none\": answers already sit inside the accordion item's padded box. Set a length to hold a long answer to a reading measure; \"none\" restores the default.", "applies_when": [{"prop": "items", "present": true}] },
      "--faq-heading-accent-color": { "type": "color", "default": "var(--color-accent)", "description": "Color of the title_accent substring within the heading, if set", "applies_when": [{"prop": "title", "present": true}] },
      "--faq-heading-margin-bottom": { "type": "length", "default": "var(--space-lg)", "description": "Space below the FAQ heading, before the accordion list (header rhythm). Lower it to tighten the header, raise it for more separation.", "applies_when": [{"prop": "title", "present": true}] },
      "--faq-question-color": { "type": "color", "default": "var(--color-text)", "description": "Question (summary) text color", "applies_when": [{"prop": "items", "present": true}] },
      "--faq-answer-color": { "type": "color", "default": "var(--color-muted)", "description": "Answer text color", "applies_when": [{"prop": "items", "present": true}] },
      "--faq-item-border-color": { "type": "color", "default": "var(--color-border)", "description": "Accordion item border color", "applies_when": [{"prop": "items", "present": true}] },
      "--faq-item-radius": { "type": "length", "default": "4px", "description": "Accordion item corner radius. faq owns this value independently — before issue 577 the rendered radius came from the grid component's card slot, which faq could neither set nor resolve.", "applies_when": [{"prop": "items", "present": true}] },
      "--faq-question-open-color": { "type": "color", "default": "var(--color-accent)", "description": "Open-state question text color and chevron color", "applies_when": [{"prop": "items", "present": true}], "conditionality_note": "the question is OPEN — the accordion's expanded state, which is interaction state rather than authored data" }
    }
  },
  "safe_to_edit": ["faq.php", "../../assets/css/components.css (COMPONENT: faq section)"],
  "do_not_touch": ["schema.json without updating README.md"]
}
